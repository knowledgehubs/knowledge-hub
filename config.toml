baseURL = "https://knowledgehubs.github.io/knowledge-hub/"
languageCode = "en-us"
title = "Knowledge Hub"
# 💡 تحديد القالب الجديد (مهم جداً!)
theme = "PaperMod" 

# 🏆 Google Analytics (الإعداد القياسي لـ Hugo)
googleAnalytics = "G-BP6F13C7B5" # <-- رمز القياس الخاص بك

# إعدادات الـ Pagination (لحل تحذيرات Hugo)
[pagination]
  pagerSize = 10
  
# إعدادات الـ Robot (هام لـ SEO)
enableRobotsTXT = true

# 🏆 التعديل الحاسم: هذا يضمن أن Hugo يستخدم الروابط الكاملة
canonifyURLs = true 

# ===============================================
# 💡 إعدادات PaperMod الإضافية (للمظهر والقائمة)
# ===============================================

[params]
  # Basic site parameters
  description = "A comprehensive network of automatically generated high-quality content."
  # تحديد عدد المقالات على الصفحة الرئيسية (Home Page)
  mainSections = ["posts"]
  paginate = 10
  
  # إعدادات PaperMod
  defaultTheme = "auto" # وضع داكن أو فاتح تلقائي
  disableThemeToggle = false
  ShowReadingTime = true
  ShowShareButtons = false
  ShowPostNavHeaders = true
  ShowBreadCrumbs = true

# إعدادات القائمة الرئيسية (Main Menu)
[[menu.main]]
  name = "Home"
  url = "/"
  weight = 10
[[menu.main]]
  name = "Articles"
  url = "/posts" # هذا هو المسار الذي تم توليد المقالات فيه
  weight = 20
[[menu.main]]
  name = "About"
  url = "/about" # يمكنك إضافة صفحة /about لاحقاً
  weight = 30

[taxonomies]
  tag = "tags"
  category = "categories"

# إعدادات الـ Markdown لتمكين قصاصات Hugo (Shortcodes) و JSON-LD
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true # يسمح بعرض أكواد HTML خام مثل JSON-LD و Shortcodes
